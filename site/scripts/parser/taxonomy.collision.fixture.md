# Fixture: the same label defined twice within one axis

## Research areas (columns)

### Alpha
The first Alpha definition, with explicit in-scope and out-of-scope criteria.

### Alpha
A second Alpha definition in the same vocabulary. Under a flat keying this one
would silently win; it must fail the build instead.
