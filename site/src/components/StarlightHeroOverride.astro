---
// Empty override for Starlight's built-in Hero component.
// Setting `hero: {}` in a page's frontmatter makes Page.astro enter the
// hero branch (suppressing the auto PageTitle <h1>), but we don't want
// Starlight's own hero UI — our custom Hero.astro in MDX replaces it.
// This component intentionally renders nothing.
---
