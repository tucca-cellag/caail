---
title: Cellular Agriculture AI Library
description: The curated library at the intersection of cellular agriculture and AI.
template: splash
tableOfContents: false
editUrl: false
hero: {}
---

import RevealScript from '../../components/RevealScript.astro';
import HomeRail from '../../components/HomeRail.astro';
import Hero from '../../components/Hero.astro';
import SectionsGrid from '../../components/SectionsGrid.astro';
import StartHere from '../../components/StartHere.astro';
import RecentlyAdded from '../../components/RecentlyAdded.astro';
import TopicsBand from '../../components/TopicsBand.astro';
import CommunityBand from '../../components/CommunityBand.astro';
import CiteBand from '../../components/CiteBand.astro';
import WhyCaail from '../../components/WhyCaail.astro';
import AskExamples from '../../components/AskExamples.astro';
import GetStarted from '../../components/GetStarted.astro';

{/* Arms the scroll-reveal system. Must precede every `.sr` element on the page so the
    hidden state is set before they are parsed, never after they have been painted. */}
<RevealScript />
<HomeRail />

<Hero />
<SectionsGrid />

{/* Human routing first, while the reader is still near the top: pick an entry point, see
    what changed. The agent story then follows as the pitch, ending on how to connect one. */}
<div class="caail-two-col" id="start">
  <StartHere />
  <RecentlyAdded />
</div>

{/* Directly after the router, and before the pitch. "Start here" asks which kind of reader
    you are; this asks whether your own subject is in the corpus, which is the question a
    working researcher actually arrives with. Both are wayfinding, so they belong together
    and above `#why`, which argues why the answer is worth trusting once you have it. */}
<TopicsBand />

{/* Order is load-bearing twice over. `#why` argues the answers are worth trusting, `#ask`
    shows four of them, and `#connect` is how you get your own — so the CTA at the foot of
    the band has the install path immediately under it. And HomeRail's scroll-spy takes the
    LAST rail entry whose offsetTop is above the viewport midpoint, walking the entries in
    list order, so rail order and document order have to agree or a tick lights on the wrong
    section. */}
<WhyCaail />
<AskExamples />
<GetStarted />

{/* Paired. Each band is one short line of copy plus two actions, so at full width both
    ran to ~1100px with most of it empty. Side by side they read as a footer pair. */}
<div class="caail-band-pair">
  <CommunityBand />
  <CiteBand />
</div>
