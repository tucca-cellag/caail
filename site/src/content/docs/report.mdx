---
title: Report an issue with an entry
description: Tell us an entry in CAAIL is wrong — a misplaced paper, a stale figure, a dead link — by GitHub, email, or Slack, carrying the entry's id so a curator can find it.
---

import ReportRoutes from '../../components/ReportRoutes.astro';

Every entry in CAAIL carries a permanent id, and every card links here with its own id
attached. Naming that id is what turns "one of the scaffolding papers looked off" into a
correction someone can act on.

<ReportRoutes />
