{
  "name": "caail-events",
  "private": true,
  "version": "0.1.0",
  "description": "Cookieless collector for CAAIL's two usage events.",
  "scripts": {
    "check": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250801.0",
    "wrangler": "^4.28.0"
  }
}
